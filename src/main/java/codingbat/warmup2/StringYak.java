package codingbat.warmup2;

public class StringYak {
    public String stringYak(String str) {
        String sub = "yak";
        return str.replace(sub, "");
    }
/*Предположим, строка «як» несчастливая. Учитывая строку, верните версию,
в которой все «як» удалены, но «а» может быть любым символом. Струны «яка»
не будут перекрываться.*/
}
